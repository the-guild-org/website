diff --git a/dist/breadcrumb.js b/dist/breadcrumb.js
index ec6ab6043..0355942cb 100644
--- a/dist/breadcrumb.js
+++ b/dist/breadcrumb.js
@@ -26,8 +26,14 @@ function getBreadcrumbItemsFromPath(tree, path, options) {
 				});
 				break;
 			case "folder":
-				if (item.root && !includeRoot) {
+				if (item.root) {
 					items = [];
+					if (includeRoot) {
+						items.push({
+							name: tree.name,
+							url: typeof includeRoot === "object" ? includeRoot.url : item.index?.url
+						});
+					}
 					break;
 				}
 				if (i === path.length - 1 || item.index !== path[i + 1]) items.push({
@@ -40,10 +46,6 @@ function getBreadcrumbItemsFromPath(tree, path, options) {
 				break;
 		}
 	}
-	if (includeRoot) items.unshift({
-		name: tree.name,
-		url: typeof includeRoot === "object" ? includeRoot.url : void 0
-	});
 	return items;
 }
 /**
diff --git a/dist/toc.js b/dist/toc.js
index ce2eece04..cceeea5c2 100644
--- a/dist/toc.js
+++ b/dist/toc.js
@@ -13,6 +13,9 @@ function mergeRefs(...refs) {
 		});
 	};
 }
+function hasLayoutBox(element) {
+	return element.getClientRects().length > 0;
+}
 
 //#endregion
 //#region src/toc.tsx
@@ -89,24 +92,28 @@ function useAnchorObserver(watch, single) {
 	const onChange = useEffectEvent((entries) => {
 		stateRef.current ??= { visible: /* @__PURE__ */ new Set() };
 		const state = stateRef.current;
-		for (const entry of entries) if (entry.isIntersecting) state.visible.add(entry.target.id);
+		for (const entry of entries) if (entry.isIntersecting && hasLayoutBox(entry.target)) state.visible.add(entry.target.id);
 		else state.visible.delete(entry.target.id);
 		if (state.visible.size === 0) {
 			const viewTop = entries.length > 0 ? entries[0]?.rootBounds?.top ?? 0 : 0;
 			let fallback;
-			let min = -1;
 			for (const id of watch) {
 				const element = document.getElementById(id);
-				if (!element) continue;
-				const d = Math.abs(viewTop - element.getBoundingClientRect().top);
-				if (min === -1 || d < min) {
-					fallback = element;
-					min = d;
+				if (!element || !hasLayoutBox(element)) continue;
+				if (element.getBoundingClientRect().top <= viewTop) fallback = element;
+				else {
+					fallback ??= element;
+					break;
 				}
 			}
 			setActiveAnchor(fallback ? [fallback.id] : []);
 		} else {
-			const items = watch.filter((item) => state.visible.has(item));
+			const items = watch.filter((item) => {
+				if (!state.visible.has(item)) return false;
+				const element = document.getElementById(item);
+				if (!element) return false;
+				return hasLayoutBox(element);
+			});
 			setActiveAnchor(single ? items.slice(0, 1) : items);
 		}
 	});
