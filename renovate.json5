{
  extends: ['github>the-guild-org/shared-config:renovate'],
  packageRules: [
    {
      // ignore Nextra to avoid conflicts with @theguild/components
      matchSourceUrls: ['https://github.com/shuding/nextra'],
      enabled: false,
    },
  ],
}
