---
interface Props {
  name: string;
  defaultOpen?: boolean;
}

const { name } = Astro.props;
---

<li>
  <span class="text-[var(--muted)]">▾</span>
  <span class="text-[var(--strong)]">{name}/</span>
  <ul><slot /></ul>
</li>
