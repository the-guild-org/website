---
title: GraphQL
description:
  Open Source developers with experience of working with the largest companies and applications.
  GraphQL consulting, workshops and trainings.
thumbnail: https://open-graph-image.theguild.workers.dev/?product=GUILD&title=GraphQL%20Tools
---

import { ClientLogosSection } from '@/client-logos-section'
import { Newsletter } from '@/components'
import { Ecosystem } from '@/ecosystem'
import { GetInTouchSection } from '@/get-in-touch-section'
import { Hero } from '@/hero'
import { RecommendedReadingSection } from '@/recommended-reading-section'
import { ServicesSection } from '@/services-section'
import { allBlogs } from '../lib/all-blogs'

export default function IndexPage() {
  return (
    <>
      {/* 
        We're missing H1,
        but we cannot display it as it's ugly and does not fit.
        So we're using an invisible H1 for SEO purposes.
      */}
      <h1 className="invisible">The Guild</h1>
      <Hero />
      <ClientLogosSection />
      <Ecosystem />
      <ServicesSection />
      <div className="nextra-container">
        <GetInTouchSection />
      </div>
      <RecommendedReadingSection articles={allBlogs.slice(0, 4)} />
      <div className="nextra-container">
        <Newsletter />
      </div>
    </>
  )
}
