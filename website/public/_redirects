# Main-site redirects. The Hive rules are appended after this block at build
# time by scripts/hive/generate-redirects.ts.
/contact /#talk-to-an-engineer 302
# There is no site-wide full text; the index links every product's own llms-full.txt.
/llms-full.txt /llms.txt 301
# The old Mesh site was reachable at /api/mesh from some docs.
/api/mesh /graphql/mesh 301
# The old graphql-tools "remote schemas" guide became a Schema Stitching page; the
# generated Tools API reference still links to the old root path.
/docs/remote-schemas /graphql/stitching/docs/getting-started/remote-subschemas 301
