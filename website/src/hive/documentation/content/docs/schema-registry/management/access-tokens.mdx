---
title: Access Tokens
---

import { Callout } from "@hive/design-system/hive-components/callout";
import { Image } from "@hive/design-system/image";
import { Step, Steps } from "#mdx-shims/steps";

import accessTokenConfirmImage from "./access-token-confirm.png";
import accessTokenCreateSuccessImage from "./access-token-create-success.png";
import accessTokenDeleteConfirmImage from "./access-token-delete-confirm.png";
import accessTokenEnterGeneralImage from "./access-token-enter-general.png";
import accessTokenMoreMenuImage from "./access-token-more-menu.png";
import accessTokenPermissionsImage from "./access-token-permissions.png";
import accessTokenResourcesImage from "./access-token-resources.png";
import accessTokensTabImage from "./access-tokens-tab-settings.png";

Access Tokens in Hive Console are secure credentials used to authenticate API requests and automate
actions without using personal login details.

With Access Tokens, you can perform tasks such as:

- Publish schemas etc. with the Hive CLI
- Report usage data (SDK or HTTP)
- Access the [public Hive Console GraphQL API](/docs/api-reference/graphql-api) for managing
  resources and exporting data

Access Tokens in Hive Console support granular permission scopes, allowing you to define exactly
what actions a token is allowed to perform and on which resources (project, target, service or app
deployment). This helps enforce the principle of least privilege, ensuring tokens only have the
access they need - and nothing more.

For example, a CI pipeline that publishes schemas only needs the "Publish Service" scope. There’s no
need to grant more permissions.

Three types of access token exist.

**Organization Access Tokens.** These are scoped at the organization level and managed by
organization administrators. They are typically used to integrate with external tooling for
provisioning resources such as projects and users.

**Project Access Tokens**: These are scoped at the project level and can be managed by any project
member with sufficient permissions. They are commonly used for self-service operations such as CI/CD
or CLI workflows.

**Personal Access Tokens (PATs).** These are scoped to individual organization members and can be
enabled or disabled on a per-user basis. They are primarily used for personal or local development
tasks, such as running `hive dev`.

<Callout type="warning">
  **Important**: Treat access tokens like passwords. Keep them secret and revoke
  them immediately if compromised. Monitor token usage regularly and revoke
  unused tokens to maintain security. And use automatic token expiration to
  ensure tokens get rotated.
</Callout>

## Access Token Type Overview

Depending on the use-case you want to use different access tokens. All the access tokens share the
following properties:

- Assigning **granular permissions**.
- Assigning **granular resources** in a **hierarchical** structure.

### Organization Access Tokens

- Require the `Manage organization access tokens` (`accessToken:modify`) permission (assignable to
  users roles).
- Can technically be used to elevate permissions, so usage should be restricted.
- Recommended **only for organization administrators**, typically for **third-party integrations**
  that provision or manage resources at the organization level.

### Project Access Tokens

- Require the `Manage project access tokens` (`projectAccessToken:modify`) permission (assignable to
  user roles).
- Manage access within a **specific project**.
- Most access tokens should fall under this category.
- Recommended for **CI/CD pipelines** performing actions such as schema publishing or schema checks.
- Organization administrators can view and revoke all project access tokens from the organization
  settings.

### Personal Access Tokens (PATs)

- Require the `Manage personal access tokens` (`personalAccessToken:modify`) permission (assignable
  to user roles).
- Organization administrators can view and revoke all personal access tokens in the organization
  settings.
- The permissions and resources of a PAT are a **subset of the member’s role permissions**.
- If a member’s role permissions are downgraded, the PAT automatically reflects the change.
- Recommended for **local CLI usage**, such as running `hive dev` or `hive check`.
- **Not recommended for CI/CD** - use Project Access Tokens instead.
- Removing a user from the organization automatically **deletes** their PATs.
- Removing the `Manage personal access tokens` (`personalAccessToken:modify`) permission
  **temporarily disables** all tokens issued by that user until the permission is restored.

## Create an Access Token

Depending on the type of access token you are creating, navigate either to the organization settings
`Access Token` subpage for creating `Organization Access Tokens`, the organization settings
`Personal Access Tokens` subpage for creating `Personal Access Tokens` or the project settings
`Access Tokens` subpage for creating `Project Access Tokens`

<Steps>
<Step>
### Ensure sufficient permissions

To create an Access Token in Hive Console, you must have the `Manage Access Token` permission at the
organization level. Tokens are created at the **organization level** and can be managed (created or
deleted) by any user with sufficient permissions.

</Step>

<Step>
### Navigate to Access Token Settings

Go to the `Settings` tab of your organization and open the `Access Tokens` section.

Click `Create new access token`.

<Image
  alt="Organization Access Tokens Section"
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokensTabImage}
/>

</Step>

<Step>
### Enter General Information

Provide a clear **title** and **description** to help your team understand the token’s purpose. Optionally select an **expiration** time to automatically expire tokens for improved security posture.

Confirm your selection by clicking `Next`.

<Image
  alt="Enter general access token information"
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenEnterGeneralImage}
/>

</Step>

<Step>
### Select Permission Scopes

Choose the permissions the token should have.

Confirm your selection by clicking `Next`.

<Image
  alt="Select permission scopes for the access token"
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenPermissionsImage}
/>

<Callout>
  **Tip**: Follow the principle of least privilege by only selecting the scopes
  necessary for the token's purpose.
</Callout>

</Step>

<Step>
### Select Resources

Decide if the token should have access to:

- **All resources** in the organization, or
- Only **specific projects/targets/services/apps**.

Confirm your selection by clicking `Next`.

<Image
  alt="Select resources for the access token"
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenResourcesImage}
/>

</Step>

<Step>
### Review and Create

Carefully review all the details—title, description, expiration, scopes, and resource assignments.

When you're ready, click `Create Access Token`.

<Image
  alt="Confirm permissions and scopes on the access token"
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenConfirmImage}
/>

</Step>

<Step>
### Save your Token

After creation, your **access token key** will be displayed once.

- Copy the token and store it securely (for example, in a secrets manager).
- You won't be able to view it again.
- Use this token to authenticate with Hive's CLI or API.
- **Never share your token** and immediately revoke it if it becomes compromised.

<Image
  alt="Confirm permissions and scopes on the access token"
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenCreateSuccessImage}
/>
</Step>
</Steps>

## Deleting Access Tokens

Within the Access Token overview, click the three dots icon for opening the context menu and within that menu, click the `Delete` menu item..

<Image
  alt="Open the actions for the access token by pressing the three dot icon on the end of the row."
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenMoreMenuImage}
/>

Confirm the `Delete Access Token` within the dialog.

<Image
  alt="Confirm the deletion within the dialogue."
  className="mt-8 rounded-lg drop-shadow-md"
  src={accessTokenDeleteConfirmImage}
/>

## Updating Access Tokens

Updating access tokens is currently not possible. Please instead
[create a new access token](/docs/schema-registry/management/access-tokens#create-an-access-token) with updated
permissions, replace the usages of the old access token and then
[delete the old access token](/docs/schema-registry/management/access-tokens#deleting-access-tokens).
