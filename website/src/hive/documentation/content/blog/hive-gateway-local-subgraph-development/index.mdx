---
title: "Hive Gateway: Local Development and Gateway Testing Just Got Easier"
tags: [platform, graphql, graphql-federation, graphql-hive, hive-gateway]
authors: [jdolle]
date: 2026-09-16
description:
  "Hive Gateway's new dev supergraph source composes the subgraphs on your machine with the rest of
  your graph, so you can develop and test end to end with one process."
---

import { Callout } from "@hive/design-system/hive-components/callout";
import { Tabs } from "@hive/design-system/tabs";

You are adding a field to the `products` subgraph and wiring it into the checkout page. The
resolver is a ten line change. Proving that it works is not: the field only means something once
it is composed into the supergraph and served by the gateway, next to the `reviews`, `inventory`
and `users` subgraphs that other teams own and that you do not want to run on your laptop.

Hive Gateway now has a supergraph source built for exactly this loop. Tell the gateway which
subgraphs are running locally, and it composes them with the rest of your graph and serves the
result. One process, the real gateway, your change included.

## The loop today

Federated teams usually end up with one of two setups for local development.

The first is running everything. Clone every subgraph, start them all, introspect them, compose,
then start the gateway. It works, but it is heavy, and the copies of the subgraphs you did not touch
drift away from what is deployed the moment someone else merges.

The second is [`hive dev`](/docs/api-reference/cli#local-subgraph-development). The Hive CLI takes
the schema that is published to a target, adds or replaces your local subgraph, composes, and writes
a `supergraph.graphql` file that a local gateway can watch. This is much lighter, because the
registry already knows every other subgraph. It is still a second process and a file in between,
and a second terminal you have to remember to start.

## A `dev` supergraph source

The new source folds that second process into the gateway. In your `gateway.config.ts`, or straight
from the CLI:

<Tabs items={['Configuration File', 'CLI']}>

{/* Configuration File */}

<Tabs.Tab>

```ts title="gateway.config.ts"
import { defineConfig } from '@graphql-hive/gateway'

export const gatewayConfig = defineConfig({
  supergraph: {
    type: 'dev',
    services: [
      // the subgraph you are working on, running on your machine
      { name: 'products', url: 'http://localhost:4001/graphql' }
    ],
    // compose through the Hive registry with the rest of the graph
    remote: true,
    registry: 'https://app.graphql-hive.com/graphql',
    token: process.env.HIVE_REGISTRY_TOKEN!,
    target: {
      bySelector: {
        organizationSlug: 'my-org',
        projectSlug: 'my-project',
        targetSlug: 'staging'
      }
    }
  }
})
```

</Tabs.Tab>

{/* CLI */}

<Tabs.Tab>

```sh
hive-gateway supergraph \
  --dev-service products=http://localhost:4001/graphql \
  --dev-remote \
  --dev-registry https://app.graphql-hive.com/graphql \
  --dev-registry-token "$HIVE_REGISTRY_TOKEN" \
  --dev-target my-org/my-project/staging
```

</Tabs.Tab>

</Tabs>

On start, the gateway fetches the SDL of each listed service, composes a supergraph, and serves it.
On every polling interval it fetches the SDLs again and recomposes only when one of them changed,
so you can leave it running while you edit resolvers and schema. Save the change, and the next poll
picks it up.

Each service can provide its schema in the way that suits it:

- `federation` (the default) queries the subgraph's `_service { sdl }` field.
- `graphql` runs a standard introspection query, for services that are not federation aware.
- `file` reads an SDL file from disk. The `url` is still where the gateway will route requests, but
  the schema comes from the file, which means you can compose a schema before a single resolver
  exists.

## Compose locally, or through the registry

The `remote` flag is the interesting part, and it is where Hive's view of schema governance shows
up in a local development feature.

Without `remote`, the gateway composes the listed services and nothing else, entirely on your
machine. It is instant, it works offline, and it is a good fit for prototyping or for the case
where you really do have several subgraphs running locally and want them composed together.

With `remote`, the gateway sends the SDLs of your local services to the Hive registry, which
composes them with the latest composable schema version published to the target you point at. Your
local `products` replaces the registry's `products`; every other subgraph comes from the registry
as it was last published. You only run what you are changing.

So which one? Use remote composition when:

- your graph is published to a Hive target and you want the result to match what schema checks
  and production will see
- you want to run only the services you are changing, with everything else taken from the
  registry as last published
- unrelated subgraphs should always be their latest published versions, so schema drift shows up
  while you are still editing, not after you open the pull request

Use local composition when:

- you are offline or prototyping without a registry
- every subgraph you need is already running locally
- you want the fastest possible feedback loop

One difference matters more than it looks: the composition engine. Composition is a strict process
with a lot of rules, and those rules evolve. If every developer composes on their own machine, every
developer is running their own version of the composer. When the registry composes, the exact same
engine validates your work locally, on the pull request, and on the way to production. What
composes on your laptop composes in CI.

Our recommendation is simple: use `remote` whenever you have a registry. Keep local composition for
offline work and quick experiments.

## Why this is a full-stack feature

The people who feel this the most are not the ones who only own a subgraph. They are the ones who
change a subgraph *and* the client that consumes it in the same afternoon.

With the dev source, the frontend talks to `http://localhost:4000/graphql` like it always does,
except the gateway behind it serves your modified graph. Code generation runs against the composed
schema, so the new field shows up in your generated types. The query your UI actually sends goes
through the real query planner, hits your local subgraph for the part you changed, and hits the
rest of the graph as published. When you are done, nothing about your workflow changes: you open
the pull request, `hive schema:check` runs against the same registry, and the same composition
engine gives the same answer it gave your gateway all afternoon.

A few things fall out of that naturally:

- **Several local subgraphs.** List two or three `--dev-service` entries and they all replace
  their published counterparts in one composition.
- **Schema first.** Point a service at an SDL file with `source: 'file'`, compose it with the real
  graph, and build the client against the result before the resolvers exist.
- **One terminal.** No separate composition process and no intermediate supergraph file to keep in
  sync.

<Callout type="info">
  `hive dev` is not going anywhere. If you need a supergraph file on disk, for another gateway, for
  tooling, or for a CI job, it remains the right tool. The dev source is for the case where the
  gateway itself is the consumer.
</Callout>

## Governance does not stop at the laptop

Hive treats the schema registry as the single source of truth for a graph: schema checks with
usage-based breaking change detection, contracts, versioned history, promotions and rollbacks all
hang off it. A local development feature that composed a private, possibly stale copy of the graph
with a possibly different composer would sit awkwardly next to that.

The dev source is designed so that it does not. In remote mode the registry does the composing,
against the schema versions it holds, with the engine it will use for the pull request. The gateway
only adds the part that is yours: the subgraphs on your machine. The loop gets shorter; the
guarantees stay where they were.

## Try it

The dev supergraph source is available in Hive Gateway today. The
[documentation](/docs/gateway/supergraph-proxy-source) covers the configuration options, the CLI
flags and environment variables, and how each service source resolves its schema.

If you try it on your graph, we would like to hear how it fits your workflow. Open an issue or a
discussion in the [Hive Gateway repository](https://github.com/graphql-hive/gateway).
